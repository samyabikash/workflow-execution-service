server:
  port: 8080

spring:
  application:
    name: workflow-execution-service

logging:
  level:
    com.growthloops.workflow: INFO

workflow:
  demo:
    delay-millis: 10000   # 10 seconds — plenty of time to catch RUNNING in Postman